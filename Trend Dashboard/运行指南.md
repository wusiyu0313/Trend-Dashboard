# Trend Dashboard 运行指南

## 📋 前置要求

确保您的系统已安装：
- **Node.js** (版本 18 或更高)
- **npm** (通常随 Node.js 一起安装)

## 🚀 运行步骤

### 方法一：使用命令行（推荐）

1. **打开 PowerShell 或命令提示符**

2. **进入项目目录**
   ```powershell
   cd C:\Users\13641\trend-dashboard
   ```

3. **安装依赖**
   ```powershell
   npm install
   ```
   ⚠️ 如果遇到 PowerShell 执行策略问题，可以使用：
   ```powershell
   cmd /c "npm install"
   ```

4. **启动开发服务器**
   ```powershell
   npm run dev
   ```
   或使用：
   ```powershell
   cmd /c "npm run dev"
   ```

5. **打开浏览器**
   访问：http://localhost:3000

### 方法二：使用 VS Code

1. 在 VS Code 中打开项目文件夹
2. 打开终端（Ctrl + `）
3. 运行 `npm install`
4. 运行 `npm run dev`
5. 点击终端中的链接或手动访问 http://localhost:3000

## 📝 常用命令

- `npm install` - 安装所有依赖
- `npm run dev` - 启动开发服务器
- `npm run build` - 构建生产版本
- `npm start` - 运行生产版本
- `npm run lint` - 运行代码检查

## ⚠️ 常见问题

### 问题1：PowerShell 执行策略错误
**解决方案**：
- 使用 `cmd /c "npm install"` 代替 `npm install`
- 或者以管理员身份运行 PowerShell，执行：
  ```powershell
  Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
  ```

### 问题2：端口 3000 已被占用
**解决方案**：
- 关闭占用 3000 端口的程序
- 或修改 `package.json` 中的 dev 脚本为：
  ```json
  "dev": "next dev -p 3001"
  ```

### 问题3：依赖安装失败
**解决方案**：
- 清除 npm 缓存：`npm cache clean --force`
- 删除 `node_modules` 文件夹和 `package-lock.json`
- 重新运行 `npm install`

## 🎉 成功运行后

您应该看到：
- 浏览器显示 Trend Dashboard 界面
- 5 个平台的趋势卡片
- 每个卡片显示模拟的趋势数据
- 可以点击刷新按钮更新数据

## 📚 下一步

- 查看 `README.md` 了解如何集成真实 API
- 查看 `.env.example` 了解需要配置的环境变量
- 修改 `app/api/` 目录下的文件以集成真实数据源

祝您使用愉快！🎊

